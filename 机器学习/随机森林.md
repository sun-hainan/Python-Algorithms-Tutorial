# 随机森林

_三个臭皮匠，赛过诸葛亮_

---

## 📖 学习目标

1. 理解决集成学习的思想
2. 掌握 Bagging 的原理
3. 理解决策树的集成方法

---

## 第一部分：什么是随机森林？

### 核心思想

```
随机森林 = 决策树 + 随机化 + 集成

多个决策树投票决定结果
```

### 结构

```
随机森林
    │
    ├── 决策树 1 → 预测: A类
    ├── 决策树 2 → 预测: B类
    ├── 决策树 3 → 预测: A类
    └── ...

最终结果：A类 (多数投票)
```

---

## 第二部分：Bagging（Bootstrap Aggregating）

```python
def bootstrap_sample(X, y):
    indices = np.random.randint(0, len(X), size=len(X))
    return X[indices], y[indices]
```

---

## 第三部分：随机森林代码

```python
class RandomForest:
    def __init__(self, n_trees=100):
        self.n_trees = n_trees

    def fit(self, X, y):
        self.trees = []
        for _ in range(self.n_trees):
            X_boot, y_boot = bootstrap_sample(X, y)
            tree = DecisionTree()
            tree.fit(X_boot, y_boot)
            self.trees.append(tree)

    def predict(self, X):
        predictions = np.array([tree.predict(X) for tree in self.trees])
        return np.apply_along_axis(
            lambda x: np.bincount(x).argmax(), axis=0, arr=predictions)
```

---

## ✅ 小结

1. **随机森林**是决策树的集成
2. **Bagging**减少方差，提高稳定性
3. **多数投票**汇总结果

---

_继续学习：下一章「K-Means聚类」_
